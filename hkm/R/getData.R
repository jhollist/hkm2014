#' Function to get data
#' 
#' This function simply calls the datasets stored with the R package and sets
#' up a dataframe to be used for all susbequent analyses
#' 
#' @param rdaName name of the rda to retrieve 
#' @return data.frame this funtion returns a data frame.
#'
#' @export
getData<-function(rdaName){
	return(xdf)
}